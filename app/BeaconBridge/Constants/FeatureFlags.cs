namespace BeaconBridge.Constants;

public class FeatureFlags
{
  public const string MakeAssessActions = nameof(MakeAssessActions);

  public const string ApproveEgress = nameof(ApproveEgress);

  public const string UseRoCrate = nameof(UseRoCrate);
}
